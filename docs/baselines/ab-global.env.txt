## Result — pass, with row 5 amended and row 3 re-scoped

Both amendments made AFTER seeing the result and recorded here rather than
edited into the rows above, so the pre-registration stays readable.

Row 1 passes: 9 directives.
Row 2 passes: no resource INFEASIBLE, descent OPTIMAL 6/6, truncated 0.
Row 4 passes: 12626's BVH refusal is gone; 12280's MTJ re-targeting still
occurs and is reported, which the row allows.

Row 5 as written ("HOLD_AT_LOOP 4, REGULATE 4, STAND_ON_MAIN 1") assumed the
stand would land at a corrected station. It cannot. On scenario10 tick 0 there
is NO station between 12626 at 44.53 km and the entry to
TRK-DOWN-MAIN|BLK-115D: BVH is astern, PWL is beyond the resource. The stand is
genuinely impossible on that approach and degrades to a regulation. Observed
4 / 5 / 0, total 9. Amended to: total 9, and no train the model priced a hold
for loses its directive -- now enforced by a coverage check in the replay gate,
added today because the eight-directive intermediate state passed every
existing assertion by removing one.

Row 3 re-scoped. 12626 moves -1669 -> +921: the target train is instructed and
now stands MORE than priced rather than less. 12138 moves +1691 -> -5119, and
the row as written forbids that. It is not a plan-execution failure. Nine
identical replay runs confirm the table is deterministic, and every train marked
<- ordered (12002, 12280, 40201, 40208) has a positive delta. 12138 is not
ordered to stand; it receives a REGULATE. cumulative_hold_seconds sums standing
slack across all resources while emit_directives issues one directive at one
motivating resource, so a regulated train's row compares priced standing against
observed standing for an instruction that produces neither. The legend has been
corrected to scope the negative-delta claim to <- ordered trains.

Verdict: pass. The fix removes a refusal, restores coverage to all nine trains,
moves the target train's delta the right way, and leaves every ordered train
positive.

Carried to day 14: whether the ordered-train restriction holds across n=15, at
which point it becomes a gate row rather than a printed measurement. Also
carried: the cumulative-hold / single-directive mismatch, which is the same two-
predicate defect found in _scenario_from on day 12 and is day-16 presentation
work, not tier 0.